import { Editor } from '@tiptap/react';
import React from 'react';
export type TextMenuProps = {
    editor: Editor;
};
export declare const TextMenu: ({ editor }: TextMenuProps) => React.JSX.Element;
