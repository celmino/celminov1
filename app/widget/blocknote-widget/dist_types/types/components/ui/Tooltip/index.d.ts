/// <reference types="react" />
import { TooltipProps } from './types';
export declare const Tooltip: ({ children, enabled, title, shortcut, tippyOptions, }: TooltipProps) => JSX.Element;
export default Tooltip;
