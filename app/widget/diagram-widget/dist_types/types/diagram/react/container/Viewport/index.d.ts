import type { ReactNode } from 'react';
import React from 'react';
type ViewportProps = {
    children: ReactNode;
};
export declare function Viewport({ children }: ViewportProps): React.JSX.Element;
export {};
