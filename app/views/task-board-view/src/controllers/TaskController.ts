
import { HStack, UIController, UIView, UIWidget, VStack, cLeading, cTopLeading, useParams, useQueryParams, Text, UIRouteOutlet } from "@tuval/forms";

import { views } from "../Views";
import { EventBus } from "@tuval/core";



export class TaskController extends UIController {
    public LoadView(): UIView {
       
        return (
            Text('dsf')
        )
    }
}