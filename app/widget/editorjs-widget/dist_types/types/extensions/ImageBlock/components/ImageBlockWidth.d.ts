import React from 'react';
export type ImageBlockWidthProps = {
    onChange: (value: number) => void;
    value: number;
};
export declare const ImageBlockWidth: React.MemoExoticComponent<({ onChange, value }: ImageBlockWidthProps) => React.JSX.Element>;
